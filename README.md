### database: MS SQL, Redis(cache function)
### Only invoices CRUD API
### The content of service should be repository, I put it in wrong page.

start Redis in docker
```
docker run -d --name redis -p 6379:6379 redis:7
```
